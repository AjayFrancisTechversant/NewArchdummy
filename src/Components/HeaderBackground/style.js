import {StyleSheet} from 'react-native';

const styles = () =>
  StyleSheet.create({
    linearGradient: {
      width: '100%',
      height: '100%',
    },
    singleFlex: {
      flex: 1,
    },
    childContainer: {position: 'absolute'},
  });

export default styles;
