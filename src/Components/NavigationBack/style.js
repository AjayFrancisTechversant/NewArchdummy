import {StyleSheet} from 'react-native';

const styles = () =>
  StyleSheet.create({
    outerContainer: {
      paddingLeft: 300 / 30,
    },
    imageCover: {
      width: 20,
      height: 20,
    },
  });

export default styles;
